{
  "name": "@alitajs/workspace",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && lerna bootstrap",
    "clean": "lerna clean",
    "publish": "lerna publish",
    "build": "lerna run build",
    "prerelease": "yarn build",
    "release": "yarn --pure-lockfile && walrus release"
  },
  "license": "MIT",
  "devDependencies": {
    "@walrus/cli": "^0.3.1",
    "@walrus/pansy": "^0.2.3",
    "@walrus/plugin-lerna-release": "^1.0.3",
    "lerna": "^3.18.4",
    "lerna-changelog": "^0.8.3",
    "typescript": "^3.7.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=10.9.0",
    "yarn": ">=1.3.2"
  }
}
